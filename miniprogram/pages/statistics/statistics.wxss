/* pages/statistics/statistics.wxss */
.container {
  padding: var(--spacing-xl);
  background: linear-gradient(135deg, var(--background-color) 0%, #FFFAF0 100%);
  min-height: 100vh;
  font-family: var(--font-family);
}

.header {
  text-align: center;
  margin-bottom: var(--spacing-xl);
  padding: var(--spacing-lg) 0;
  background-color: var(--card-background);
  border-radius: var(--radius-xl);
  padding: var(--spacing-xl);
  box-shadow: var(--shadow-md);
  margin-bottom: var(--spacing-xl);
}

.title {
  font-size: 40rpx;
  font-weight: bold;
  color: var(--text-primary);
  letter-spacing: 1rpx;
  background: linear-gradient(135deg, var(--primary-color) 0%, #FF9500 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* 加载状态 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xxxl) 0;
  background-color: var(--card-background);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  margin: var(--spacing-lg) 0;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 6rpx solid rgba(255, 122, 69, 0.2);
  border-top: 6rpx solid var(--primary-color);
  border-radius: var(--radius-circle);
  animation: spin 1s linear infinite;
  margin-bottom: var(--spacing-lg);
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  font-size: 28rpx;
  color: var(--text-secondary);
  font-weight: 500;
}

/* 错误状态 */
.error-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xxxl) var(--spacing-xl);
  background-color: var(--card-background);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  margin: var(--spacing-lg) 0;
  text-align: center;
}

.error-icon {
  font-size: 60rpx;
  margin-bottom: 20rpx;
}

.error-text {
  font-size: 28rpx;
  color: var(--error-color);
  margin-bottom: var(--spacing-xl);
  line-height: 1.6;
}

.retry-btn {
  padding: var(--spacing-md) 60rpx;
  background: linear-gradient(135deg, var(--primary-color) 0%, #FF8C66 100%);
  color: white;
  border: none;
  border-radius: var(--radius-md);
  font-size: 28rpx;
  font-weight: 600;
  box-shadow: var(--shadow-primary);
  transition: all var(--transition-base);
}

.retry-btn:active {
  transform: translateY(0) scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(255, 122, 69, 0.4);
}

/* 数据容器 */
.data-container {
  background-color: transparent;
  padding: 20rpx 0;
}

/* 今日学习统计卡片 */
.today-stats-section {
  margin-bottom: 40rpx;
  padding: 0 20rpx;
}

.today-stats-card {
  background: linear-gradient(135deg, #ffffff 0%, #fafafa 100%);
  border-radius: 20rpx;
  padding: 40rpx 30rpx;
  box-shadow: 0 6rpx 20rpx rgba(0, 0, 0, 0.08);
  position: relative;
  overflow: hidden;
  border: 2rpx solid transparent;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.today-stats-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 8rpx;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
}

.today-stats-header {
  text-align: center;
  margin-bottom: 30rpx;
  position: relative;
  z-index: 1;
}

.today-stats-title {
  display: block;
  font-size: 36rpx;
  font-weight: bold;
  color: var(--text-primary);
  margin-bottom: 8rpx;
  background: linear-gradient(135deg, var(--primary-color) 0%, #FF9500 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.today-stats-subtitle {
  display: block;
  font-size: 24rpx;
  color: var(--text-secondary);
  font-weight: 500;
}

.today-stats-content {
  position: relative;
  z-index: 1;
}

.today-time-display {
  display: flex;
  align-items: baseline;
  justify-content: center;
  margin-bottom: 32rpx;
  padding: 36rpx 20rpx;
  background: linear-gradient(135deg, rgba(255, 122, 69, 0.1) 0%, rgba(255, 140, 102, 0.06) 100%);
  border-radius: 20rpx;
  border: 2rpx solid rgba(255, 122, 69, 0.2);
  box-shadow: 0 4rpx 12rpx rgba(255, 122, 69, 0.1);
  position: relative;
  overflow: hidden;
}

.today-time-display::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 6rpx;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 50%, #FF9500 100%);
}

.today-time-value {
  font-size: 80rpx;
  font-weight: bold;
  background: linear-gradient(135deg, #FF7A45 0%, #FF8C66 50%, #FF9500 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-right: 12rpx;
  line-height: 1;
  letter-spacing: -2rpx;
  position: relative;
  z-index: 1;
}

.today-time-unit {
  font-size: 28rpx;
  color: var(--text-secondary);
  font-weight: 500;
  position: relative;
  z-index: 1;
}

.today-stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
  margin-bottom: 30rpx;
}

.today-stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx 12rpx;
  background: linear-gradient(135deg, rgba(255, 122, 69, 0.06) 0%, rgba(255, 140, 102, 0.03) 100%);
  border-radius: 16rpx;
  border: 2rpx solid rgba(255, 122, 69, 0.1);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.today-stat-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 4rpx;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.today-stat-item:active {
  transform: scale(0.98);
  background: linear-gradient(135deg, rgba(255, 122, 69, 0.1) 0%, rgba(255, 140, 102, 0.06) 100%);
}

.today-stat-item:active::before {
  opacity: 1;
}

.today-stat-icon {
  font-size: 32rpx;
  margin-bottom: 10rpx;
  opacity: 0.9;
}

.today-stat-value {
  font-size: 32rpx;
  font-weight: bold;
  background: linear-gradient(135deg, #FF7A45 0%, #FF8C66 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 4rpx;
  line-height: 1.2;
  display: inline-block;
}

.today-stat-unit {
  font-size: 20rpx;
  color: var(--text-tertiary);
  font-weight: 400;
  margin-left: 4rpx;
  display: inline-block;
  vertical-align: baseline;
}

.today-stat-label {
  font-size: 22rpx;
  color: var(--text-secondary);
  font-weight: 500;
  text-align: center;
  margin-top: 4rpx;
  line-height: 1.4;
}

.today-progress-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 2rpx solid rgba(255, 122, 69, 0.1);
}

.today-progress-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 18rpx;
}

.today-progress-label {
  font-size: 28rpx;
  color: var(--text-primary);
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.today-progress-label::before {
  content: '📊';
  font-size: 24rpx;
}

.today-progress-text {
  font-size: 36rpx;
  font-weight: bold;
  background: linear-gradient(135deg, #FF7A45 0%, #FF8C66 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 1rpx;
}

.today-progress-bar-wrapper {
  width: 100%;
  height: 20rpx;
  background: linear-gradient(90deg, #F0F0F0 0%, #E8E8E8 100%);
  border-radius: 10rpx;
  overflow: hidden;
  box-shadow: inset 0 2rpx 6rpx rgba(0, 0, 0, 0.08);
  position: relative;
}

.today-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 50%, #FF9500 100%);
  border-radius: 10rpx;
  transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 2rpx 8rpx rgba(255, 122, 69, 0.5);
  position: relative;
  overflow: hidden;
}

.today-progress-bar::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, transparent 0%, rgba(255, 255, 255, 0.3) 50%, transparent 100%);
  animation: shimmer 2s infinite;
}

@keyframes shimmer {
  0% {
    transform: translateX(-100%);
  }
  100% {
    transform: translateX(100%);
  }
}

/* 统计概览卡片 */
.stats-overview {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
  margin-bottom: 40rpx;
  padding: 0 20rpx;
}

.stat-card {
  background: linear-gradient(135deg, #ffffff 0%, #fafafa 100%);
  padding: 40rpx 20rpx;
  border-radius: 20rpx;
  box-shadow: 0 6rpx 20rpx rgba(0, 0, 0, 0.08);
  text-align: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  border: 2rpx solid transparent;
}

.stat-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 8rpx;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
}

.stat-card:hover {
  transform: translateY(-6rpx);
  box-shadow: 0 12rpx 32rpx rgba(0, 0, 0, 0.15);
  border-color: rgba(255, 122, 69, 0.2);
}

.stat-value {
  display: block;
  font-size: 56rpx;
  font-weight: bold;
  margin-bottom: 12rpx;
  background: linear-gradient(135deg, #FF7A45 0%, #FF8C66 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  transition: all 0.3s ease;
}

.stat-value.correct {
  background: linear-gradient(135deg, #52C41A 0%, #73D13D 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-value.wrong {
  background: linear-gradient(135deg, #FF4D4F 0%, #FF7875 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-label {
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
  letter-spacing: 0.5rpx;
}

/* 数据验证提示 */
.validation-info {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 20rpx;
  background-color: rgba(82, 196, 26, 0.1);
  border: 2rpx solid rgba(82, 196, 26, 0.3);
  border-radius: 16rpx;
  margin: 0 20rpx 30rpx;
  animation: fadeIn 0.5s ease;
}

.validation-icon {
  font-size: 32rpx;
}

.validation-text {
  font-size: 26rpx;
  color: #52C41A;
  font-weight: 500;
}

/* 动画效果 */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 无数据状态 */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100rpx 40rpx;
  background-color: #fff;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  margin: 20rpx 0;
  text-align: center;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 32rpx;
  font-weight: bold;
  color: #666;
  margin-bottom: 16rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: #999;
  line-height: 1.5;
}

/* 头部更新时间 */
.update-time {
  display: block;
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
  font-weight: normal;
}

/* 统计卡片图标 */
.stat-icon {
  font-size: 40rpx;
  margin-bottom: 12rpx;
  display: block;
}

.stat-card.correct::before {
  background: linear-gradient(90deg, #52C41A 0%, #73D13D 100%);
}

.stat-card.wrong::before {
  background: linear-gradient(90deg, #FF4D4F 0%, #FF7875 100%);
}

.stat-card.accuracy::before {
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
}

.stat-card.time::before {
  background: linear-gradient(90deg, #1890FF 0%, #40A9FF 100%);
}

.stat-card.study::before {
  background: linear-gradient(90deg, #722ED1 0%, #9254DE 100%);
}

/* 数据验证警告 */
.validation-warning {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 20rpx;
  background-color: rgba(255, 193, 7, 0.1);
  border: 2rpx solid rgba(255, 193, 7, 0.3);
  border-radius: 16rpx;
  margin: 0 20rpx 30rpx;
  animation: fadeIn 0.5s ease;
}

/* 标签切换 */
.tabs-container {
  display: flex;
  gap: 16rpx;
  padding: 0 20rpx;
  margin-bottom: 30rpx;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.tab-item {
  flex: 1;
  min-width: 120rpx;
  padding: 20rpx 30rpx;
  text-align: center;
  background: #fff;
  border-radius: 12rpx;
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
  transition: all 0.3s ease;
  border: 2rpx solid transparent;
  white-space: nowrap;
}

.tab-item.active {
  background: linear-gradient(135deg, #FF7A45 0%, #FF8C66 100%);
  color: #fff;
  border-color: #FF7A45;
  box-shadow: 0 4rpx 12rpx rgba(255, 122, 69, 0.3);
}

/* 标签内容区域 */
.tab-content {
  animation: fadeInUp 0.5s ease;
}

/* 图表卡片 */
.chart-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 32rpx;
  margin: 0 20rpx 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  animation: fadeInUp 0.6s ease;
}

.chart-header {
  margin-bottom: 32rpx;
  border-bottom: 2rpx solid #f0f0f0;
  padding-bottom: 16rpx;
}

.chart-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

/* 趋势图表 */
.trend-chart {
  display: flex;
  justify-content: space-around;
  align-items: flex-end;
  height: 300rpx;
  gap: 16rpx;
}

.trend-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
}

.trend-bar-container {
  flex: 1;
  width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  gap: 4rpx;
  margin-bottom: 12rpx;
}

.trend-bar {
  width: 100%;
  border-radius: 4rpx 4rpx 0 0;
  transition: height 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growUp 0.8s ease-out;
}

.correct-bar {
  background: linear-gradient(180deg, #52C41A 0%, #73D13D 100%);
}

.wrong-bar {
  background: linear-gradient(180deg, #FF4D4F 0%, #FF7875 100%);
}

.trend-label {
  font-size: 24rpx;
  color: #999;
  margin-bottom: 4rpx;
}

.trend-value {
  font-size: 22rpx;
  color: #666;
  font-weight: 500;
}

/* 详细趋势图表 */
.trend-detail-chart {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.trend-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.trend-info {
  flex: 0 0 180rpx;
  display: flex;
  flex-direction: column;
}

.trend-date {
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 8rpx;
}

.trend-stats {
  font-size: 24rpx;
  color: #999;
}

.trend-bar-wrapper {
  flex: 1;
}

.trend-progress-bar {
  height: 24rpx;
  background: #f0f0f0;
  border-radius: 12rpx;
  overflow: hidden;
  position: relative;
}

.trend-progress {
  height: 100%;
  transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growWidth 0.8s ease-out;
}

.correct-progress {
  background: linear-gradient(90deg, #52C41A 0%, #73D13D 100%);
}

/* 学习时长图表 */
.study-time-chart {
  display: flex;
  justify-content: space-around;
  align-items: flex-end;
  height: 250rpx;
  gap: 16rpx;
}

.time-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
}

.time-bar-container {
  flex: 1;
  width: 100%;
  display: flex;
  align-items: flex-end;
  margin-bottom: 12rpx;
}

.time-bar {
  width: 100%;
  background: linear-gradient(180deg, #1890FF 0%, #40A9FF 100%);
  border-radius: 4rpx 4rpx 0 0;
  transition: height 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growUp 0.8s ease-out;
}

.time-label {
  font-size: 24rpx;
  color: #999;
  margin-bottom: 4rpx;
}

.time-value {
  font-size: 22rpx;
  color: #666;
  font-weight: 500;
}

/* 正确率分布 */
.accuracy-distribution {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.accuracy-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.accuracy-item:active {
  background: #f5f5f5;
}

.accuracy-bar-container {
  flex: 1;
  height: 32rpx;
  background: #f0f0f0;
  border-radius: 16rpx;
  overflow: hidden;
  position: relative;
}

.accuracy-bar {
  height: 100%;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
  border-radius: 16rpx;
  transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growWidth 0.8s ease-out;
}

.accuracy-label {
  flex: 0 0 80rpx;
  font-size: 26rpx;
  color: #666;
  font-weight: 500;
  text-align: center;
}

.accuracy-count {
  flex: 0 0 80rpx;
  font-size: 24rpx;
  color: #999;
  text-align: right;
}

/* 题型分布 */
.type-distribution {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.type-item {
  padding: 20rpx;
  background: #fafafa;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.type-item:active {
  background: #f0f0f0;
  transform: scale(0.98);
}

.type-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12rpx;
}

.type-name {
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
}

.type-accuracy {
  font-size: 26rpx;
  color: #FF7A45;
  font-weight: 500;
}

.type-stats {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.type-bar-container {
  flex: 1;
  height: 24rpx;
  background: #f0f0f0;
  border-radius: 12rpx;
  overflow: hidden;
  display: flex;
  position: relative;
}

.type-bar {
  height: 100%;
  transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growWidth 0.8s ease-out;
}

.type-bar.correct {
  background: linear-gradient(90deg, #52C41A 0%, #73D13D 100%);
}

.type-bar.wrong {
  background: linear-gradient(90deg, #FF4D4F 0%, #FF7875 100%);
}

.type-count {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
  min-width: 80rpx;
  text-align: right;
}

/* 错题分布 */
.wrong-distribution {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.wrong-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.wrong-item:active {
  background: #f5f5f5;
}

.wrong-point {
  flex: 0 0 120rpx;
  font-size: 26rpx;
  color: #333;
  font-weight: 500;
}

.wrong-count-wrapper {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.wrong-count-bar {
  flex: 1;
  height: 32rpx;
  background: linear-gradient(90deg, #FF4D4F 0%, #FF7875 100%);
  border-radius: 16rpx;
  transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growWidth 0.8s ease-out;
}

.wrong-count {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
  min-width: 60rpx;
  text-align: right;
}

/* 知识点掌握程度 */
.knowledge-distribution {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.knowledge-item {
  padding: 24rpx;
  background: linear-gradient(135deg, #fafafa 0%, #ffffff 100%);
  border-radius: 16rpx;
  border-left: 6rpx solid #FF7A45;
  transition: all 0.3s ease;
}

.knowledge-item:active {
  transform: translateX(4rpx);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.knowledge-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.knowledge-name {
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
}

.knowledge-mastery {
  font-size: 32rpx;
  font-weight: bold;
  color: #FF7A45;
}

.knowledge-progress-container {
  height: 16rpx;
  background: #f0f0f0;
  border-radius: 8rpx;
  overflow: hidden;
  margin-bottom: 12rpx;
}

.knowledge-progress {
  height: 100%;
  background: linear-gradient(90deg, #FF7A45 0%, #FF8C66 100%);
  border-radius: 8rpx;
  transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
  animation: growWidth 1s ease-out;
}

.knowledge-detail {
  font-size: 24rpx;
  color: #999;
}

.knowledge-stats {
  font-size: 24rpx;
  color: #999;
}

/* 动画效果 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes growUp {
  from {
    height: 0;
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes growWidth {
  from {
    width: 0;
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* 卡片入场动画 */
.stat-card {
  animation: fadeInUp 0.6s ease-out both;
}

/* 响应式设计 */
@media (max-width: 375rpx) {
  .stats-overview {
    grid-template-columns: repeat(2, 1fr);
    gap: 16rpx;
  }
  
  .stat-card {
    padding: 30rpx 16rpx;
  }
  
  .stat-value {
    font-size: 48rpx;
  }
  
  .tabs-container {
    gap: 12rpx;
  }
  
  .tab-item {
    padding: 16rpx 24rpx;
    font-size: 26rpx;
  }
  
  .chart-card {
    padding: 24rpx;
    margin: 0 16rpx 20rpx;
  }
  
  .trend-chart {
    height: 250rpx;
  }
  
  .study-time-chart {
    height: 200rpx;
  }
  
  /* 今日学习统计响应式 */
  .today-stats-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 12rpx;
  }
  
  .today-stat-item {
    padding: 16rpx 8rpx;
  }
  
  .today-stat-icon {
    font-size: 28rpx;
  }
  
  .today-stat-value {
    font-size: 28rpx;
  }
  
  .today-stat-unit {
    font-size: 18rpx;
  }
  
  .today-stat-label {
    font-size: 20rpx;
  }
  
  .today-time-value {
    font-size: 64rpx;
  }
  
  .today-time-unit {
    font-size: 24rpx;
  }
}

/* 平板和桌面端优化 */
@media (min-width: 768rpx) {
  .container {
    max-width: 1200rpx;
    margin: 0 auto;
  }
  
  .stats-overview {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .tabs-container {
    justify-content: center;
  }
  
  .tab-item {
    flex: 0 0 auto;
    min-width: 150rpx;
  }
}